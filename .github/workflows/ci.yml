name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    name: build
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - run: xcodebuild -project Wallpaper.xcodeproj -scheme Wallpaper -configuration Release build -derivedDataPath build -quiet CODE_SIGNING_ALLOWED=NO
